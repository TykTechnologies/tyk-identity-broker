# Generated by: gromit policy

# Check the documentation at http://goreleaser.com
# This project needs CGO_ENABLED=1 and the cross-compiler toolchains for
# - arm64
# - amd64
version: 2
builds:
  - id: std-amd64
    env:
      - NOP=nop # ignore this, it is jsut to avoid a complex conditional in the templates
    ldflags:
      - -X github.com/TykTechnologies/tyk-identity-broker/main.Version={{.Version}}
      - -X github.com/TykTechnologies/tyk-identity-broker/main.Commit={{.FullCommit}}
      - -X github.com/TykTechnologies/tyk-identity-broker/main.BuildDate={{.Date}}
      - -X github.com/TykTechnologies/tyk-identity-broker/main.BuiltBy=goreleaser
    goos:
      - linux
    goarch:
      - amd64
    binary: tyk-identity-broker
  - id: std-arm64
    env:
      - NOP=nop # ignore this, it is jsut to avoid a complex conditional in the templates
    ldflags:
      - -X github.com/TykTechnologies/tyk-identity-broker/main.Version={{.Version}}
      - -X github.com/TykTechnologies/tyk-identity-broker/main.Commit={{.FullCommit}}
      - -X github.com/TykTechnologies/tyk-identity-broker/main.BuildDate={{.Date}}
      - -X github.com/TykTechnologies/tyk-identity-broker/main.BuiltBy=goreleaser
    goos:
      - linux
    goarch:
      - arm64
    binary: tyk-identity-broker
  - id: std-s390x
    env:
      - NOP=nop # ignore this, it is jsut to avoid a complex conditional in the templates
    ldflags:
      - -X github.com/TykTechnologies/tyk-identity-broker/main.Version={{.Version}}
      - -X github.com/TykTechnologies/tyk-identity-broker/main.Commit={{.FullCommit}}
      - -X github.com/TykTechnologies/tyk-identity-broker/main.BuildDate={{.Date}}
      - -X github.com/TykTechnologies/tyk-identity-broker/main.BuiltBy=goreleaser
    goos:
      - linux
    goarch:
      - s390x
    binary: tyk-identity-broker
nfpms:
  - id: std
    vendor: "Tyk Technologies Ltd"
    homepage: "https://tyk.io"
    maintainer: "Tyk <info@tyk.io>"
    description: Tyk Authentication Proxy for third-party login
    package_name: tyk-identity-broker
    file_name_template: "{{ .ConventionalFileName }}"
    ids:
      - std-amd64
      - std-arm64
      - std-s390x
    formats:
      - deb
      - rpm
    contents:
      - src: "README.md"
        dst: "/opt/share/docs/tyk-identity-broker/README.md"
      - src: "ci/install/*"
        dst: "/opt/tyk-identity-broker/install"
      - src: ci/install/inits/systemd/system/tyk-identity-broker.service
        dst: /lib/systemd/system/tyk-identity-broker.service
      - src: ci/install/inits/sysv/init.d/tyk-identity-broker
        dst: /etc/init.d/tyk-identity-broker
      - src: "LICENSE.md"
        dst: "/opt/share/docs/tyk-identity-broker/LICENSE.md"
      - src: tib_sample.conf
        dst: /opt/tyk-identity-broker/tib.conf
        type: "config|noreplace"
    scripts:
      preinstall: "ci/install/before_install.sh"
      postinstall: "ci/install/post_install.sh"
      postremove: "ci/install/post_remove.sh"
    bindir: "/opt/tyk-identity-broker"
    rpm:
      scripts:
        posttrans: ci/install/post_trans.sh
      signature:
        key_file: tyk.io.signing.key
    deb:
      signature:
        key_file: tyk.io.signing.key
        type: origin
publishers:
  - name: std
    ids:
      - std
    env:
      - PACKAGECLOUD_TOKEN={{ .Env.PACKAGECLOUD_TOKEN }}
    cmd: packagecloud publish --debvers "{{ .Env.DEBVERS }}" --rpmvers "{{ .Env.RPMVERS }}" tyk/tyk-identity-broker-unstable {{ .ArtifactPath }}
# This disables archives
archives:
  - formats: ['binary']
    allow_different_binary_count: true
checksum:
  disable: true
release:
  disable: true
  github:
    owner: TykTechnologies
    name: tyk-identity-broker
  prerelease: auto
  draft: true
  name_template: "{{.ProjectName}}-v{{.Version}}"
