on: 
  workflow_dispatch:
jobs:
  scanCVE:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 1
      - name: Change directory and Make
        run: |
          cd ci/CVEs
          make



           