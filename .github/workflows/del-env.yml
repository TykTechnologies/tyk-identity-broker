# Generated by: tyk-ci/wf-gen
# Generated on: Mon 26 Apr 05:06:31 UTC 2021

# Generation commands:
# ./pr.zsh -title ready for release -branch releng/install -base releng/install -repos tyk-identity-broker -p
# m4 -E -DxREPO=tyk-identity-broker

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
