# Generated by: tyk-ci/wf-gen
# Generated on: Wed  9 Jun 00:59:01 UTC 2021

# Generation commands:
# ./pr.zsh -title remove jessie -branch releng/manifests -repos tyk-identity-broker -base releng/manifests -p
# m4 -E -DxREPO=tyk-identity-broker

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
