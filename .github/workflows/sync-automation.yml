

# Generated by: tyk-ci/wf-gen
# Generated on: Fri Mar 11 14:26:29 UTC 2022

# Generation commands:
# ./meta.zsh -base TD-879/Sync-releng-templates-master -branch TD-879/Sync-meta-automation-master -title internal: Sync automation templates -repos tyk-identity-broker
# m4 -E -DxSRC_BRANCH='master' -DxRELEASE_BRANCHES='release-1.2, release-1.3' -DxAUTO_FILES='ci .github/workflows/release.yml .github/dependabot.yml .github/workflows/del-env.yml .github/workflows/api-tests.yml .github/workflows/ui-tests.yml'

# This workflow will silently ignore FILES that are missing in master
# Most of the time this is what we want, older repos may not have some files

name: Sync automation

on:
  push:
    branches:
      - master
    paths:
      - .github/workflows/release.yml
      - .github/workflows/api-tests.yml
      - .github/workflows/ui-tests.yml
      - ci/**

env:
  FILES: ci .github/workflows/release.yml .github/dependabot.yml .github/workflows/del-env.yml .github/workflows/api-tests.yml .github/workflows/ui-tests.yml

jobs:
  sync:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        branch: [ release-1.2, release-1.3 ]

    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{matrix.branch}}
          token: ${{ secrets.ORG_GH_TOKEN }}

      - name: sync ${{matrix.branch}} from master
        run: |
          git fetch origin master
          git config --local user.email "wf-gen@tyk-ci"
          git config --local user.name "Bender"
          for f in $FILES; do 
            git checkout --theirs origin/master -- $f || true
          done
          git commit -a -m "[CI] Syncing release automation from master"
          git push origin
          echo "::debug::${FILES} syncd for ${{matrix.branch}}"
